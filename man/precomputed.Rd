% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{precomputed}
\alias{precomputed}
\alias{binary_example}
\alias{continuous_example}
\alias{survival_example}
\alias{custom_example}
\title{Precomputed sample-size simulations}
\format{
A list of class \code{"pmsims"}. The most useful components are:
\describe{
\item{\code{min_n}}{The estimated minimum sample size.}
\item{\code{perf_n}}{Expected performance at \code{min_n}.}
\item{\code{target_performance}}{The performance target the search aimed for.}
\item{\code{metric}}{The performance metric used.}
\item{\code{model}}{The model fitted to each simulated dataset.}
\item{\code{mlpwr_ds}}{The sampled designs and simulated performance values
behind the fitted curve, used by \code{\link[=plot]{plot()}}.}
\item{\code{summaries}}{Aggregated performance summaries across replications.}
\item{\code{simulation_time}}{Elapsed time of the original run, in seconds.}
}

Objects produced by the wrapper functions additionally record the
data-generating configuration (for example \code{outcome_prevalence},
\code{correlation} and \code{complexity}); see \code{\link[=simulate_binary]{simulate_binary()}} for the full set.

An object of class \code{pmsims} of length 36.

An object of class \code{pmsims} of length 35.

An object of class \code{pmsims} of length 37.

An object of class \code{pmsims} of length 24.
}
\source{
Generated by \code{data-raw/precomputed-examples.R}.

\code{binary_example}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{set.seed(123)
simulate_binary(
  signal_parameters = 20,
  noise_parameters = 0,
  complexity = 1,
  data_control = list(correlation = 0.3),
  outcome_prevalence = 0.30,
  maximum_achievable_cstatistic = 0.80,
  model = "glm",
  metric = "calibration_slope",
  target_performance = 0.85,
  n_reps_total = 1000,
  mean_or_assurance = "assurance"
)
}\if{html}{\out{</div>}}

\code{continuous_example}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{set.seed(123)
simulate_continuous(
  signal_parameters = 15,
  noise_parameters = 0,
  complexity = 1,
  data_control = list(correlation = 0.3),
  maximum_achievable_rsquared = 0.50,
  model = "lm",
  metric = "calibration_slope",
  target_performance = 0.95,
  n_reps_total = 1000,
  mean_or_assurance = "assurance"
)
}\if{html}{\out{</div>}}

\code{survival_example}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{set.seed(123)
simulate_survival(
  signal_parameters = 15,
  noise_parameters = 0,
  complexity = 1,
  data_control = list(correlation = 0.3),
  maximum_achievable_cindex = 0.70,
  baseline_hazard = 0.01,
  censoring_rate = 0.30,
  model = "coxph",
  metric = "calibration_slope",
  target_performance = 0.90,
  n_reps_total = 1000,
  mean_or_assurance = "assurance"
)
}\if{html}{\out{</div>}}

\code{custom_example}: a linear model with three independent predictors and a
population \eqn{R^2} of 0.5, targeting a calibration slope of 0.9 with
80\% assurance. The search uses bounds of 25 to 1,000 participants, 1,000
replications and 30,000 test observations. See \code{\link[=simulate_custom]{simulate_custom()}} for the
generating functions and executable call (seed 123).
}
\usage{
binary_example

continuous_example

survival_example

custom_example
}
\description{
Results from four completed sample-size searches, one for each of the
package's entry points. A search repeatedly simulates datasets, fits a
prediction model to each and evaluates its performance across a range of
candidate sample sizes, so a realistic run takes minutes to hours. These
objects were computed once, with the calls shown under \emph{Source}, so that the
examples and the vignette can demonstrate the output without recomputing it.
}
\details{
Each object is a \code{"pmsims"} object, as returned by \code{\link[=simulate_binary]{simulate_binary()}},
\code{\link[=simulate_continuous]{simulate_continuous()}}, \code{\link[=simulate_survival]{simulate_survival()}} and \code{\link[=simulate_custom]{simulate_custom()}}, and
can be used with \code{\link[=print]{print()}} and \code{\link[=plot]{plot()}} in the usual way.
}
\examples{
binary_example
binary_example$min_n
}
\seealso{
\code{\link[=simulate_binary]{simulate_binary()}}, \code{\link[=simulate_continuous]{simulate_continuous()}},
\code{\link[=simulate_survival]{simulate_survival()}}, \code{\link[=simulate_custom]{simulate_custom()}}
}
\keyword{datasets}
