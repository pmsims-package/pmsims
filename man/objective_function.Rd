% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmsims_objective_function.R
\name{objective_function}
\alias{objective_function}
\title{Objective function for pmsims}
\usage{
objective_function(
  n,
  penalty_weight,
  target_performance,
  min_sample_size,
  max_sample_size,
  value_on_error
)
}
\arguments{
\item{n}{Integer sample size.}

\item{penalty_weight}{Penalty weight \eqn{\lambda} on the cost term \eqn{C(n)};
\code{0} minimises only the absolute performance gap.}

\item{target_performance}{Target performance \eqn{M^*} (numeric).}

\item{min_sample_size}{Minimum sample size considered (lower bound of search).}

\item{max_sample_size}{Maximum sample size considered (upper bound of search).}

\item{value_on_error}{Value to return if the objective cannot be evaluated.}
}
\value{
A single numeric: the objective value at \eqn{n}.
}
\description{
Computes the objective value
\deqn{\left| M(n) - M^* - \lambda\,C(n) \right|,}
where \eqn{M(n)} is performance at sample size \eqn{n}, \eqn{M^*} is the
target performance, \eqn{C(n)} is a cost term increasing with \eqn{n}, and
\eqn{\lambda} (set via \code{penalty_weight}) controls the penalty on large samples.
}
\examples{
# \dontrun{
# Example usage (assuming helper functions exist):
# objective_function(
#   n = 500,
#   penalty_weight = 0.1,
#   target_performance = 0.75,
#   min_sample_size = 100,
#   max_sample_size = 5000,
#   value_on_error = Inf
# )
# }
}
\keyword{internal}
