% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary_tuning.R
\name{binary_tuning}
\alias{binary_tuning}
\title{Tuning function for a binary outcome model}
\usage{
binary_tuning(
  target_prevalence,
  target_performance,
  candidate_features,
  proportion_noise_features,
  complexity = 1,
  nonlinear_strength = NULL,
  correlation = 0.3,
  distribution = "normal",
  predictor_type = "continuous",
  binary_prevalence = 0,
  n_sim = 3e+05,
  n_validate = NULL,
  beta_interval = c(1e-04, 20),
  beta_tol = 1e-04,
  mu_tol = 1e-06,
  tolerance = 0.02
)
}
\arguments{
\item{target_prevalence}{Target outcome prevalence. Must be in (0, 1).}

\item{target_performance}{Target AUC (c-statistic). Must be in (0.5, 1).}

\item{candidate_features}{Total number of predictors (signal + noise).}

\item{proportion_noise_features}{Proportion of \code{candidate_features}
that are noise predictors. Must be in [0, 1).}

\item{complexity}{Integer 1-4. Default = 1.}

\item{nonlinear_strength}{Fraction of signal variance carried by the
nonlinear component (C2/C3 only), in [0, 1). When \code{NULL} (default), the
complexity-level default is used: C1 = 0, C2 = 0.2, C3 = 0.3, C4 = 0.}

\item{correlation}{Common pairwise predictor correlation. Defaults to the
SAME value as \code{generate_binary_data()} so the two never silently
disagree. Whatever you pass here must match the generator call.}

\item{distribution}{Global continuous predictor distribution family.
Defaults to match \code{generate_binary_data()}.}

\item{predictor_type}{\code{"continuous"} (default) or \code{"binary"}.
Defaults to match \code{generate_binary_data()}.}

\item{binary_prevalence}{Bernoulli probability for binary predictors.
Defaults to match \code{generate_binary_data()}.}

\item{n_sim}{Sample size for the internal LP simulation. Larger values give
more stable AUC estimates within the bisection. Default = 300 000.}

\item{n_validate}{Sample size for the independent end-to-end validation.
Defaults to \code{n_sim}.}

\item{beta_interval}{Search interval \code{c(lo, hi)} for beta_signal.
Default = \code{c(1e-4, 20)}. The upper bound is doubled automatically
if the AUC at \code{hi} is still below \code{target_performance}.}

\item{beta_tol}{Absolute convergence tolerance for the beta bisection.
Default = 1e-4.}

\item{mu_tol}{Absolute convergence tolerance for the mu_lp root-finding.
Default = 1e-6.}

\item{tolerance}{Acceptable absolute deviation of the validated prevalence
and AUC from their targets before a warning is raised. Default = 0.02.}
}
\value{
A named numeric vector:
\describe{
\item{\code{mu_lp}}{Tuned log-odds intercept.}
\item{\code{beta_signal}}{Tuned effect size.}
\item{\code{prevalence_achieved}}{Prevalence from an independent
generate_binary_data() validation draw.}
\item{\code{auc_achieved}}{AUC of the oracle LP from the same draw.}
\item{\code{var_lp_unit}}{Estimated Var(LP) at beta_signal = 1.}
}
}
\description{
Finds the \code{beta_signal} and log-odds intercept (\code{mu_lp}) that
jointly produce the target AUC and prevalence under the exact
data-generating settings (complexity, nonlinear_strength, correlation,
distribution, predictor_type).
}
\keyword{internal}
